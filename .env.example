# Copy this file to ".env" and fill in your real values (self-host only).
# DO NOT commit your real ".env" file.

# OpenAI (if you self-host and want to inject via env)
OPENAI_API_KEY=

# Optional placeholders used in the sanitized workflow
SLACK_CHANNEL_ID=
GOOGLE_SHEET_DOC_ID=
